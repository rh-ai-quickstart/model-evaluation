{{- if .Values.api.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.api.name }}
  labels:
    {{- include "ai-quickstart-template.api.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.api.replicas }}
  selector:
    matchLabels:
      {{- include "ai-quickstart-template.api.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ai-quickstart-template.api.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "ai-quickstart-template.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: api
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ include "ai-quickstart-template.image" (dict "name" .Values.api.image.repository "tag" .Values.api.image.tag "Values" .Values) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            {{- if .Values.database.enabled }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-quickstart-template.fullname" . }}-secret
                  key: DATABASE_URL
            {{- end }}
            - name: DEBUG
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-quickstart-template.fullname" . }}-secret
                  key: DEBUG
            - name: ALLOWED_HOSTS
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-quickstart-template.fullname" . }}-secret
                  key: ALLOWED_HOSTS
            - name: API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-quickstart-template.fullname" . }}-secret
                  key: API_TOKEN
            - name: MAAS_ENDPOINT
              value: {{ .Values.models.maasEndpoint | quote }}
            - name: MODEL_A_NAME
              value: {{ .Values.models.modelA.name | quote }}
            - name: MODEL_A_DEPLOYMENT_MODE
              value: {{ .Values.models.modelA.deploymentMode | quote }}
            - name: MODEL_B_NAME
              value: {{ .Values.models.modelB.name | quote }}
            - name: MODEL_B_DEPLOYMENT_MODE
              value: {{ .Values.models.modelB.deploymentMode | quote }}
            - name: EMBEDDING_MODEL
              value: {{ .Values.models.embeddingModel | quote }}
            - name: JUDGE_MODEL_NAME
              value: {{ .Values.models.judgeModel | quote }}
          {{- if .Values.api.healthCheck.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.api.healthCheck.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.api.healthCheck.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.api.healthCheck.liveness.periodSeconds }}
          readinessProbe:
            httpGet:
              path: {{ .Values.api.healthCheck.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.api.healthCheck.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.api.healthCheck.readiness.periodSeconds }}
          {{- end }}
          resources:
            {{- toYaml .Values.api.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
